# Admin panel authentication — override via Kubernetes Secret in production
AUTH_USERNAME=admin
AUTH_PASSWORD=changeme

# Camera RTSP URL — required, stored as a secret (contains credentials)
RTSP_URL=rtsp://user:password@192.168.1.x:554/cam/realmonitor?channel=1&subtype=0
