# Generated by kubeseal — safe to commit.
#
# To regenerate after changing k8s/secret.yaml:
#   kubeseal --format yaml < k8s/secret.yaml > k8s/sealed-secret.yaml
#
# If you don't have kubeseal installed:
#   brew install kubeseal
#   kubeseal --fetch-cert --controller-name=sealed-secrets-controller \
#             --controller-namespace=kube-system > pub-cert.pem
#   kubeseal --cert pub-cert.pem --format yaml < k8s/secret.yaml > k8s/sealed-secret.yaml
#
# ── PLACEHOLDER — run kubeseal to replace this file ──────────────────────────
# (This placeholder prevents ArgoCD from failing on a missing SealedSecret.
#  Replace with the real sealed output before applying to the cluster.)
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: people-recognition-secret
  namespace: people-recognition
spec:
  encryptedData:
    AUTH_USERNAME: PLACEHOLDER
    AUTH_PASSWORD: PLACEHOLDER
  template:
    metadata:
      name: people-recognition-secret
      namespace: people-recognition
    type: Opaque
