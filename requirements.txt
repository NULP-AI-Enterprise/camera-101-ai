# torch + torchvision are installed in Dockerfile via CPU-only index URL:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# Do NOT add them here — pip would reinstall the CUDA build (~2 GB).

# ── Video / image ─────────────────────────────────────────────────────────────
av>=12.0
opencv-python-headless>=4.9
numpy>=1.24
Pillow>=10.0

# ── Face recognition ──────────────────────────────────────────────────────────
insightface>=0.7.3
onnxruntime>=1.17

# ── Person detection (YOLO — depends on torch installed above) ─────────────────
ultralytics>=8.2
lapx>=0.5.5      # ByteTrack linear assignment (provides `import lap` compat)
scipy>=1.11      # Hungarian assignment (linear_sum_assignment) for IOUTracker

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy>=2.0

# ── Web server ────────────────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9
